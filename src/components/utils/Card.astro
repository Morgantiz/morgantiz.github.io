<div class="card">
  <div class="card-header">
    <slot name="header" />
  </div>
  <div class="card-body">
    <slot />
  </div>
</div>

<style>
  .card {
    border-radius: 1rem;
    box-shadow: 0 4px 12px white;
    padding: 1.5rem;
  }

  .card:hover {
    box-shadow: 0 4px 12px var(--accent-hover);
  }
</style>
